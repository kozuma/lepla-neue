# CLAUDE.md — Lepla 開発憲法

このファイルは Lepla の開発における恒久的な判断基準である。
セッションをまたいでも、ここに書かれた前提と規律に従うこと。
不明点や矛盾を発見した場合は、勝手に解釈せず必ず開発者(Hiroshi)に確認する。

---

## 1. プロジェクト概要

**Lepla** は、アイデンティティ変容を核とした学習プラットフォーム。

- ユーザーは「アーキタイプ(理想像)」を選び、日々の学習をその理想への**投票**として積み重ねる(James Clear『Atomic Habits』に基づく)
- 「〇〇を学ぶ」ではなく「〇〇な人になる」。知識定着はその結果である
- プロダクトの美学は「**静かな美術館**」— ゲームセンターの対極

## 2. 開発体制

- **「Hiroshi + AI(Claude Code)」のソロ開発体制**
- 最優先事項は**最速の試行錯誤**。技術的完璧さより、変更しやすさ・把握しやすさ・戻しやすさ
- リポジトリ一つ・デプロイ一つ・言語一つ(TypeScript)で全体が一つのコンテキストに収まることを守る

## 3. 不可侵事項(Non-negotiables)

以下はいかなる実装判断でも侵してはならない。

1. **アーキタイプの設計思想**
   - アーキタイプは「becoming(〜になる)」の道具であり、「you are(あなたは〜だ)」という診断・固定ではない
   - 文言・UI・APIの命名すべてで「変容・道・旅」の語彙を使い、タイプ分類の語彙を避ける
   - レアアーキタイプは「目指すもの」ではなく「授かるもの」。解放条件は原則秘匿し、グラインド(条件の逆算・作業化)を誘発する表示をしない
2. **静かな美術館**
   - ストリーク煽り・FOMO通知・ランキング・派手な演出を実装しない
   - 静けさ・余白・落ち着きを損なう提案はしない
3. **投票の質重視**
   - 量(回数)だけでなく質(強度)を評価する設計を維持する
   - 流し見が高評価になる実装をしない
4. **二層構造**
   - 数値の露出は最小限に。ユーザーに見せるのは物語(段階、称号、メッセージ)であり、生の集計値ではない
   - 詳細メトリクスは内部で記録するが、APIレスポンス・UIに安易に露出しない
   - 「データがあるから表示する」は禁止。表示には物語上の必然性が要る

## 4. 技術スタック(確定事項)

| 領域 | 採用 | 備考 |
|---|---|---|
| フレームワーク | Next.js (App Router) | Vercel にデプロイ |
| 管理画面 / CMS | Payload CMS 3.x | Next.js にマウント。**コンテンツ管理専用** |
| ビジネスロジック API | Route Handlers | Payload を経由しない軽量ルート |
| ORM | Drizzle | スキーマは TypeScript、`drizzle-kit` でマイグレーション |
| バリデーション | Zod | `drizzle-zod` によるスキーマ導出も可 |
| DB | Supabase (Postgres) | 本番。ローカルは素の Postgres |
| 認証 | Supabase Auth | 学習者側。Payload Auth は管理側のみ |
| ストレージ / 画像 | Cloudflare R2 + Images | ローカル開発では接続しない |
| テスト | Vitest | `services/` の純粋関数を最優先でテスト |
| パッケージ管理 | pnpm | |

### 採用しない技術と理由(再提案しないこと)

- **Nest.js**: チーム向けの規律強制フレームワーク。ソロ + AI ではセレモニーがコスト
- **Prisma**: 独自エンジン・不透明なクエリ生成。Drizzle の予測可能性を優先
- **tRPC**: モバイル・MCP 連携を見据え、素の REST の潰しの効きを優先
- **Supabase Edge Functions**: コードベースの分裂を避ける
- **Cloudflare Workers 上での Next.js 実行**: エッジケース対応の時間税がソロ開発では重い。将来のコスト最適化フェーズで再検討

## 5. アーキテクチャの規律

### 役割分担

- **Payload**: TopicStructure / CardTemplate / デッキ / アーキタイプ定義などの**コンテンツ管理のみ**
- **Route Handlers**: 学習セッション・投票・進捗などの**動的データすべて**。Payload の ORM 層を通さない
- 学習体験のデータフローに Payload を関与させない。この線引きを崩さない

### ディレクトリ規約

```
src/
├─ app/
│   ├─ (payload)/admin/     # Payload 管理画面
│   └─ api/                 # Route Handlers
├─ db/
│   └─ schema.ts            # Drizzle スキーマ(単一の真実)
├─ services/                # 純粋関数のビジネスロジック
│   ├─ voting.ts            # 投票強度計算
│   └─ session.ts           # セッション・成長段階判定
└─ components/
```

### コーディング規律

- **`services/` は純粋関数のみ**。DB アクセス・fetch・副作用を持ち込まない。ここが Lepla の哲学の核であり、Vitest でテストを書く価値が最も高い
- **Route Handler は4行構成**を守る: 認証チェック → Zod でパース → service 呼び出し → レスポンス。ハンドラにロジックを直書きしない
- **バッチ処理**(投票集計など)は Vercel Cron から Route Handler を叩く。同一コードベースに同居させる
- 新しい環境変数を追加したら **必ず `.env.example` にも追記**する

### デザイン・コピー規約

- スタイルは**デザイントークン**(`globals.css` / `tailwind.config.js`)経由で指定する。ハードコードした色・任意の値(arbitrary values)を使わない。詳細は `STYLE_GUIDE.md` に従う
- 基調パレット: 生成り(#F4F1E8)/ 墨(#2A2833)/ 金茶(#8F7340)。夜パレットは `[data-theme="night"]`
- コピーライティングは二つのレジスターを区別する:
  - **機能的ラベル**(ナビ・ボタン・見出し): 平易で読みやすい日本語
  - **儀式的・物語的な場面**(オンボーディング、アーキタイプ描写、成長段階名、完了メッセージ): 荘重な文体

## 6. ローカル開発環境

**原則: クローンして `pnpm dev` 一発で全部動く。クラウド依存ゼロ。**

- DB は Docker Compose の Postgres 1コンテナのみ(`docker compose up -d`)
- Supabase CLI のフル環境は使わない(重い)。本番 Supabase も「ただの Postgres」として扱うため、差分は最小
- 画像はローカルでは `public/` 配下。R2/Images への接続は環境変数で切り替え
- **`pnpm db:reset && pnpm db:seed` でいつでも初期状態に戻れること**。シードにはアーキタイプ定義・サンプルデッキ・テスト用カードを含める。これは AI との高速試行錯誤のセーブポイントである
- Node バージョンは `.node-version` で固定

## 7. 旧コードベースの扱い

**旧リポジトリは参照資料であり、移植元ではない。**

- バックエンドのコードは一行も持ち込まない(学校向けプランの残骸・試行錯誤の跡が含まれるため)
- 参照してよいのは `docs/salvage/` の3文書のみ:
  - `API_CONTRACT.md` — 要件の考古学資料。**新 API の設計図ではない**。新スキーマはアーキタイプシステム設計書を正とし、旧 API は取りこぼし照合用
  - `FRONTEND_ASSETS.md` — 持ち出し許可リスト。リスト外のコンポーネントを旧リポジトリから移植しない
  - `DOMAIN_KNOWLEDGE.md` — コードに埋まっていた暗黙のビジネスルール
- 学校向け機能(グループ・招待・教師ロール等)は凍結済み。実装しない・スキーマに含めない

## 8. やらないことリスト

- ストリーク数の強調表示、ランキング、リーダーボード
- FOMO を煽るプッシュ通知
- 投票数・スコアなど生の集計値の UI 露出(二層構造に反する)
- レアアーキタイプの解放条件の表示・進捗バー化
- 「とりあえずデータを表示する」ダッシュボード的 UI
- 上記スタックの再選定の提案(§4 の理由を読むこと)

## 9. 作業の進め方

- セッション開始時: 対象タスクを確認し、**まず実装方針を提示して承認を得てから実装**する
- タスク完了時: 計画文書(`docs/` 配下)のステータスを更新する
- 事実と推測を区別して報告する。確認できていないことを断定しない
- 迷ったら止まって聞く。特に §3 不可侵事項に関わる判断は独断しない
