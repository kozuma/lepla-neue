# Lepla Style Guide

> このファイルはUI実装時の判断基準。CLAUDE.md から参照するか、内容をそのまま転記すること。
> すべてのスタイル値は `globals.css` のトークンから取る。**ハードコードされたhex値・px値・インラインスタイルは禁止。**

## コンセプト: 静かな美術館

生成りの紙と墨。神秘性は色の暗さではなく、明朝体・広い字間・大きな余白で表現する。
金泥写経のように、紙の上に置かれた金(金茶)は常にひとつだけ——展示室で照らされる一点の作品のように。

迷ったら引き算する。装飾を足す前に「これは沈黙より雄弁か?」と問う。

## 2つのレジスタ(最重要ルール)

すべての画面・コンポーネントは、まずどちらのレジスタかを判定してから作る。

### 儀式的レジスタ(Ceremonial)
- **対象**: オンボーディング、アーキタイプ選択・詳細、学習開始/完了メッセージ、成長・解放演出、ホームの理想像表示
- **書体**: `font-ceremonial`(Noto Serif JP)+ `tracking-ceremonial` + `leading-ceremonial`
- **レイアウト**: 中央揃え。上下余白は `--space-24` 以上。1画面に主役はひとつだけ
- **コピー**: 神秘的・古風な文体(『伝説のオウガバトル』調)。「今日の一歩」が基準点
- **動き**: `duration-ceremonial`(900ms)でゆっくり現れる。フェード+わずかな上昇のみ

### 機能的レジスタ(Functional)
- **対象**: デッキ一覧、検索、設定、教材作成、フォーム、ナビゲーション
- **書体**: `font-functional`(Noto Sans JP)+ 標準字間・行間
- **レイアウト**: 左揃え。実用的な密度でよいが、余白は `--space-4` 未満にしない
- **コピー**: 平易で読みやすい日本語。能動態。「保存する」「続きを学ぶ」
- **動き**: `duration-quick`(200ms)。装飾的アニメーションは入れない

ひとつの画面に両レジスタが同居してよい(例: 学習画面 = 儀式的ヘッダー + 機能的ナビゲーション)。

## 色のルール

1. 背景は `bg-base`、カードは `bg-surface`、その上の要素は `bg-raised`。3層まで
2. **金(`accent`)は1画面に1要素まで**。主CTA、または今まさに重要なものだけ。金が2つあったら片方を消す
3. `accent-rare`(緋)はレアアーキタイプ解放・特別な瞬間専用。日常UIに使わない
4. アーキタイプ色(`archetype-*`)はアイコン・細い差し色専用。**面で塗らない**
5. 境界線は `line`(ほぼ見えない)を基本に。`line-strong` は区切りが必須の場所のみ
6. 純白(#fff)・純黒(#000)を地と文字に使わない。背景は生成りの紙、テキストは墨(`raised`のみ白を許可)

## タイポグラフィのルール

1. 階層は最大3段まで(例: 3xl見出し / base本文 / xsキャプション)
2. `font-latin` + `.text-latin-label` はラテン語の小ラベル専用(例: "ARCHETYPE", ロゴ)。本文に使わない
3. 儀式的画面のテキストは短く。3行を超える文章は分割するか削る
4. 太字(bold)は機能的レジスタのみ。儀式的レジスタでは字の大きさと余白で強調する

## 余白のルール

1. 「狭いかな」と思ったら2倍にする。余白は沈黙であり、Leplaの沈黙は機能
2. 儀式的画面: コンテンツ幅は最大 480px。画面を埋めようとしない
3. 要素間の余白は spacing トークンのみ使用(`space-4`, `space-8`, `space-12`...)。中間値を発明しない

## 動きのルール

1. アニメーションは「成長」「解放」「カード送り」など意味のある瞬間だけ
2. hover は色の変化(`accent` → `accent-hover`)のみ。拡大・回転・バウンスは禁止
3. `prefers-reduced-motion` は globals.css で対応済み。個別実装で上書きしない

## コンポーネント規約

- ボタン(主): `bg-accent text-on-accent rounded-md`、hover で `bg-accent-hover`
- ボタン(副): 背景なし、`text-secondary` + `border-line`、hover で `text-primary`
- カード: `bg-surface rounded-card shadow-card`。アーキタイプカードはタロット比率(約 5:8)
- 進捗表示: 数値・プログレスバーを生で出さない。「23回、この道を歩んでいます」のように言葉に変換する(二層データ構造の原則)

## 禁止事項(レビュー時チェックリスト)

- [ ] インラインスタイル / ハードコードされた色・サイズがない
- [ ] 金色の要素が1画面に2つ以上ない
- [ ] グラデーション・ネオン・絵文字の乱用がない(アーキタイプアイコンは例外)
- [ ] 「ポイント」「レベル」「ランキング」などゲームセンター語彙がない(旅・道・歩みの語彙を使う)
- [ ] ダークパターン(緊急性の演出、FOMO訴求)がない

## 品質の床

- モバイル(375px)で崩れない。儀式的画面はモバイルファーストで設計
- フォーカスリングを消さない(globals.css の `:focus-visible` に任せる)
- テキストコントラスト: `text-secondary` 以上を本文に使う(`text-disabled` は無効状態専用)
- フォントは next/font で読み込む(`Noto_Serif_JP`, `Noto_Sans_JP`, `Cormorant_Garamond`)

## アーキタイプアートワーク規約

- 形式: SVG(ベクター)。`artwork/` の5点を `public/archetypes/` に配置、またはReactコンポーネント化して使用
- 比率: 5:8(viewBox `0 0 320 512`)。タロット札の規格
- 様式の不変要素: 顔を描かない頭巾の人物 / 大アルカナ式ローマ数字(I〜V) / 二重の金縁と四隅の菱形 / アーチと床線 / 下部の銘板(和名+ラテン名)
- 配色: 衣 = 各アーキタイプ色(`--archetype-*` と同系)、紙 = #F3EFE4、墨 = #2A2833、金 = #8F7340系。金は天体・道具・縁飾りのみ
- モチーフ対応: 思想家=月+書物 / 創作家=放射星+画架 / 科学者=環状惑星+天球 / 探求者=羅針星+杖と鞄 / 賢者=三相月+珠杖
- 新規カード(レアアーキタイプ等)もこの枠組みを再利用する。成長段階の表現は縁飾り・星の増加で行い、人物の顔は最後まで描かない
